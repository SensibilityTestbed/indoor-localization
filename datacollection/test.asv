clear all;
close all;

timelist = [1,2,3,4,5,6,7,8,9];
index = [];
for time = 1:0.1:9
    index = [index, binary(timelist, time)];
    
    
end


for time = 1:0.1:9
    disp(fprintf('time: %d, index: %d', time, index[i]))
